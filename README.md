# Cifar-10.ipynb
This project implements image classification on the CIFAR-10 dataset using a pretrained VGG-16 convolutional neural network with transfer learning. The CIFAR-10 dataset is loaded and preprocessed by normalizing image pixel values and converting class labels into categorical format. S
